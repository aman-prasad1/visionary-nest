// Removed Chakra UI import as it's not installed and not used in the project
// Replaced with a simple theme object to avoid dependency issues

const config = {
  initialColorMode: 'light',
  useSystemColorMode: false,
};

const theme = { config };

export default theme;
